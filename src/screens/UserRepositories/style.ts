// style.ts (UserRepositories)
import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#131016",
    padding: 24,
  },
  infoContainer: {
    gap: 12,
    justifyContent: "center",
    marginBottom: 12,
  },
});
